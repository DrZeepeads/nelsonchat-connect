{
  "compilerOptions": {
    /* Target and Compatibility */
    "target": "ES2020", // Use ES2020 features
    "useDefineForClassFields": true, // Use `define` semantics for class fields
    "lib": ["ES2020", "DOM", "DOM.Iterable"], // Include DOM and ES2020 libraries

    /* Module and Resolution */
    "module": "ESNext", // Use the latest ES module standard
    "moduleResolution": "bundler", // Use bundler-compatible module resolution
    "allowImportingTsExtensions": true, // Allow `.ts` extensions in imports
    "isolatedModules": true, // Treat each file as a module
    "moduleDetection": "force", // Enforce module imports/exports
    "noEmit": true, // Do not emit output files
    "jsx": "react-jsx", // Use React JSX transform

    /* Type Checking */
    "skipLibCheck": true, // Skip checking library files for performance
    "strict": false, // Disable all strict type-checking options
    "noUnusedLocals": false, // Allow unused local variables
    "noUnusedParameters": false, // Allow unused parameters
    "noImplicitAny": false, // Allow implicit `any` types
    "noFallthroughCasesInSwitch": false, // Allow fallthrough cases in switch statements

    /* Paths and Aliases */
    "baseUrl": ".", // Base URL for resolving paths
    "paths": {
      "@/*": ["./src/*"] // Alias for cleaner imports
    }
  },
  /* Files to Include */
  "include": ["src"]
}